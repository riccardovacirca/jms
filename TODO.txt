================================================================================
TODO
================================================================================
[OK] Le migrazioni dei moduli devono avere il prefisso jms_<module_name>_
--------------------------------------------------------------------------------
[OK] Cleanup contatti orfani in jms_cti_operatore_contatti: se un operatore si
disconnette (sessione_ttl scaduta) senza aver chiamato il contatto che aveva
estratto dalla coda, quel contatto rimane bloccato nella sua coda personale e
non è più raggiungibile da altri operatori.
Aggiungiamo l'elenco di questi contatti alla sezione Operatori CTI della
dashboard con un nuovo pulsante nell'elenco degli operatori che visualizza le
i contatti nella coda di ciascun operatore specificando per ogni copntatto anche
il ttl.
--------------------------------------------------------------------------------
[OK] Il post-install script dei moduli chiama l'API prima che l'app venga
ricompilata e riavviata con il nuovo modulo.
--------------------------------------------------------------------------------
[OK] Il modulo header consente a ogni modulo di aggiungere alla barra header un
proprio elemento ad esempio uno o più link o un menu dropdown.
Non è obbligatorio per un modulo aggiungere voci al menu. Se lo fa devo potere
scegliere tra diverse aree dello header.
La gui del modulo header dovrebbe prevedere a partire da sinistra le seguenti
aree: logo, link, tema, user.
Le aree link e user sono vuote.
L'area logo è localmente configurabile.
L'area tema contiene il pulsante light/dark
L'area link può essere usata da tutti i moduli per aggiungere un proprio menu
dropdown o un singolo link (1 link o menu per modulo).
L'area user può essere usata da un solo modulo ovvero allo stato attuale dal
modulo user che dovrebbe essere il contesto in cui il menu dropdown
dell'utente è definito e da cui è importato. Il modulo user non è privilegiato
altri moduli possono usare la area user ma non due moduli contemporaneamente.
--------------------------------------------------------------------------------
[OK] Il titolo visualizzato alla estrema sinistra della barra header deve essere
configurabile
--------------------------------------------------------------------------------
[OK] La barra CTI dovebbe avere un padding minimale max 2px tb 6px rl.
--------------------------------------------------------------------------------
[OK] Il CTI dovrebbe avere una coda di chiamata contenente contatti.
La coda deve essere persistente.
Il formato di un contatto nella coda è lo stesso formato che attualmente viene
inviato dal CRM al CTI quando si esegue una chiamata.
Il CTI espone un endpoint per scrivere nella coda.
Il CRM usa l'endpoint per scrivere nella coda.
Il CRM può scrivere anche in modo massivo nella coda del CTI.
Il click sul pulsante Chiama estrae il prossimo contatto dalla coda e lo mette
nella sessione dell'operatore, quindi il contatto viene letto dalla sessione
per eseguire la chiamata.
La coda a livello di database è condivisa tra gli operatori.
Un contatto può essare messo nella sessione di qualsiasi operatore.
--------------------------------------------------------------------------------
[OK] Il CTI ha una funzione di chiamata continua implementata in una sezione
della barra del CTI con i simboli Play/Stop per prelevare dalla coda un
contatto, metterlo in sessione e avviare la chiamata a intervalli regolari ad
esempio uno ogni 20 secondi.
--------------------------------------------------------------------------------

Verifica che lo Stato attuale del modulo CTI sia il seguente e proponi interventi per i punti A,B,C,D:

Coda di chiamata
Tabella jms_cti_coda_chiamate, introdotta dalla migrazione
V20260407_080000__cti_queue.sql è condivisa tra tutti gli operatori.
Il formato di un contatto nella coda, campo contatto_json è JSONB.
Il formato atteso (documentato nel commento della migrazione) è:
{
  "id": <bigint|null>,
  "phone": "39123456789",
  "callback": "<url>|null",
  "data": [
    { "key": "Nome",  "value": "Mario Rossi", "type": "string" },
    { "key": "Note",  "value": "...", "type": "text"}
  ]
}
type può essere string, number, o text.
I campi id, callback e data sono opzionali; phone è obbligatorio per eseguire la chiamata.

Il CTI espone endpoint per scrivere nella coda. Due endpoint:
  • POST /api/CTI/vonage/queue — inserimento singolo ({contattoJson: string, priorita?: number})
  • POST /api/CTI/vonage/queue/bulk — inserimento massivo ({contatti: string[], priorita?:number})

Entrambi richiedono ruolo USER.

Il modulo CRM attualmente NON implementa metodi per scrivere sulla coda del CTI. Il CRM ha ancora il vecchio meccanismo: TurnoHandler cerca l'operatore tramite jms_cti_operatori.claim_account_id e gli passa il contatto direttamente via targetNumber nello store del frontend.
Non chiama nessuno degli endpoint della coda.
Quando i contatti sono messi nella coda di chiamata sono a disposizione di tutti gli operatori.
Il click su "Chiama" nella barra del CTI estrae il prossimo contatto dalla coda e lo mette nella
sessione dell'operatore, ma con un bug. QueueHandler.getNext usa session.sub() (che è l'accountId
JWT) come operatoreId nelle query su jms_cti_coda_chiamate e jms_cti_operatori.
Queste tabelle si aspettano l'id fisico dell'operatore (jms_cti_operatori.id),
non l'accountId utente. L'operatoreId corretto andrebbe prima recuperato tramite
OperatorDAO.findByClaimAccountId(accountId), come fa correttamente CallHandler.sdkToken.
Il contatto viene poi salvato in jms_cti_operatori.contatto_corrente via setContattoCorrente().

Questa è una ipotesi operativa ma non sono sicuro del flusso vorrei dettagliarlo
meglio per renderlo più chiaro e lineare.
Chiamiamo questo punto da definire il PUNTO A.

Il contatto viene letto dalla sessione per eseguire la chiamatasolo parzialmente.
getNext persiste il contatto in jms_cti_operatori.contatto_corrente (DB), ma la chiamata viene avviata
leggendo _pendingContact dallo stato del componente frontend (memoria).

Il DB non viene riletto al momento di _confirmCall. Se il browser venisse
ricaricato tra l'estrazione e la conferma, il contatto andrebbe perso dal
frontend (anche se rimane in DB).
Chiamiamo questo punto da definire il PUNTO B.

Il CTI ha una funzione di chiamata continua a intervalli regolari,
un auto-dialer (_toggleAutoDialer). Ogni 20 secondi esegue 
fetchAndShowContact(), ma mostra sempre il dialog di conferma — non avvia la
chiamata in modo completamente automatico senza interazione. L'operatore deve
comunque cliccare "Conferma" per ogni chiamata.
Chiamiamo questo punto da definire il PUNTO C.

Un contatto NON può essere messo nella sessione di qualsiasi operatore, non
tramite l'attuale implementazione. getNext assegna sempre il contatto
all'operatore autenticato che fa la richiesta. Non esiste un endpoint per
spingere un contatto nella sessione di un operatore specifico. Chiunque vuole
un contatto deve chiamare getNext per sé stesso. Questo è corretto e anche in
caso di chiamata automatica non modificherei questo flusso. verifica solo che
non ci siano bug o problemi non noti.
Chiamiamo questo punto da definire il PUNTO D.
--------------------------------------------------------------------------------
[OK]
- La gui dei moduli che richiedono una gestione fornsce una interfaccia admin.
--------------------------------------------------------------------------------
[OK] Implementare un nuovo modulo dashboard che si sviluppi nel contesto
<div id="main"></div> del layout principale e sia organizzato in due aree: una
sidebar e un main content. esattamente come avviene con il modulo header 
nel quale ogni altro modulo può aggiungere un link o un menu, anche alla sidebar
del modulo ogni modulo che richiede amministrazione e dispone di una interfaccia
amministrativa deve poter aggiungere un elementdo di accesso. un eventuale sub
menu della voce di menu se necessario, verrà organizzato nel contesto
visualizzato nel main content.
--------------------------------------------------------------------------------
[OK] Aggiungere al modulo user una interfaccia amministrativa che ha accesso
alla API completa e consente all'operatore di creare/modificare/eliminare utenti
--------------------------------------------------------------------------------
Tutte le operazioni del CTI che riguardano una chiamata dovrebbero generare
eventi. La politica generale è che se un modulo vuole accedere agli eventi
generati dal modulo CTI deve sottoscriversi come observer. il gestore di
eventi del modulo CTI notifica a tutti gli observer gli eventi generati
durante la propria operatività.
--------------------------------------------------------------------------------
[OK da testare] Verificare cosa succede se è stata avviata una sessione CTI e
viene ricaricata la pagina? cosa succede se l'operatore è in conversazione e
viene ricaricata la pagina? quale dovrebbe essere il comportamento più stabile
e conservativo?
--------------------------------------------------------------------------------
Ogni modulo deve avere una tabella settings che sono gestite da admin e root per
definire tutti gli aspetti configurabili dinamicamente. A questa tabella deve
corrispondere una pagina di gestione nella dashboard con un file relatvo
settings.js nella cartella admin della gui del modulo. i settings sono coppie
chiave/valore
--------------------------------------------------------------------------------
[OK] La cartella /workspace/jms/docs/core contiene un insieme di file che
documentano tutti i workflow che è possibile isolare dal flusso applicativo
della classe /workspace/jms/src/main/java/dev/jms/app/App.java. Per ciascuno è
definito anche un diagramma di sequenza UML con la sintassi Mermaid.
Usa questi file come modello per documentare tutti i workflow che è possibile
isolare dal flusso applicativo del modulo CTI. Il livello di granularità di
questa analisi deve essere ragionevole ovvero non troppo grossolano ne troppo
fine. Tutti i workflow vanno numerati in modo da rendere esplicita la
timeline dell'intero processo.
--------------------------------------------------------------------------------
[OK Situazione frammentata. Sovrascrittura sicura solo per alcuni file]
Sync del core e della libreira util.
Supponiamo di avere due installazioni jms su due macchine diverse. Queste
installazioni potrebbero avere nomi di progetto diversi esempio hello e hola.
supponiamo che nel progetto hello io esegua delle modifiche alle classi della
libreria /workspace/src/main/java/dev/jms/util e che propaghi queste modifiche
anche in /workspace/jms. Se eseguo il push di jms da hello con cmd git push -r
hello ed eseguo il pull di jms in hola con cmd git pull -r hello posso copiare
i file modificati, anche nel src del progetto, senza dover eseguire una nuova
installazione? Per quali file o cartelle di jms è possibile eseguire la stessa
procedura in sicurezza? Questa procedura di copia e sovrascrittura può essere
eseguita anche per i moduli da jms/module a src/main/java/dev/jms/app/module?
Credo che per App.java, una semplice copia in entrambe le direzioni, non sia
possibile perchè App.java, viene modificato dai moduli. Il mio dubbio riguarda
il fatto che una modifica a una libreria di util potrebbe implicare anche una
modifica delle dipendenze del file pom.xml. fai un quadro di questa situazione
--------------------------------------------------------------------------------
Il comando cmd app clean dovrebbe ripulire anche il contenuto della cartella
/workspace/src/main/resources/static
--------------------------------------------------------------------------------
[OK/DA TESTARE] Gestione contatti orfani/pianificati.
Esempio con 3 contatti A, B, C con ordine inserimenti A, B, C.
Clicco su connetti, vedo A, annullo, clicco su chiama, vedo A e continuo a
vedere A fino a quando non chiamo A oppure pianifico A. Se chiamo A, A viene
rimosso dalla coda operatore e dalla coda CTI. se pianifico A, A rimane nella
coda operatore. Al prossimo click su chiama viene mostrato B
fino a quando la pianificazione di A consente di vedere A.
Disonnessione da CTI con contatti nella coda operatore: A pianificato,
B non pianificato perchè la connessione è stata chiusa senza prendere una
decisione su B. Poichè l'operatore non ha avviato la chiamata verso B, allora
B va considerato orfano, mentre A è pianificato. nella dashboard B va mostrato
con l'etichetta orfano, A con l'etichetta pianificato. se però la data corrente
è maggiore della data di pianificazione anche A è orfano. Admin può decidere di
rimuovere entrambi dalla coda operatore qualunque sia il loro stato. La sessione
operatore dovrebbe sempre essere allineata con lo stato della tabella 
della coda operatori
--------------------------------------------------------------------------------
Implementare la registrazione audio delle conversazioni CTI con:
1. NCCO: aggiungere RecordAction alla conversazione in VoiceHelper
   (RecordAction.builder().format(MP3).split(CONVERSATION).channels(2)
   .eventUrl("/api/CTI/vonage/event").build())
2. DB: aggiungere colonne recording_url VARCHAR(500) e recording_uuid VARCHAR(64)
   a jms_cti_chiamate tramite nuova migration
3. EVENT: intercettare status="recording" in VoiceHelper.processEvent,
   salvare recording_url e recording_uuid sulla riga chiamata corrispondente
4. DOWNLOAD: endpoint GET /api/CTI/vonage/call/{id}/recording che:
   - legge recording_url da jms_cti_chiamate
   - scarica l'audio tramite VoiceClient.downloadRecordingRaw(url)
   - salva il file in storage nel percorso:
     recordings/<operatoreId>/<YYYY-MM-DD>/<timestamp>_<callId>.mp3
   - aggiorna jms_cti_chiamate con il path locale (colonna recording_path)
5. STORAGE: il percorso recordings/ è configurabile tramite
   CTI.vonage.recordings.path in application.properties
   (default: /app/data/CTI/recordings)
SDK: com.vonage server-sdk 9.10.2 —
  RecordAction (ncco), VoiceClient.downloadRecordingRaw(String url)
  EventWebhook.getRecordingUrl(), EventWebhook.getRecordingUuid()
--------------------------------------------------------------------------------
[OK] Verifica che sia possibile indicare incremento e descremento
volume/guadagno di audio e microfono nella barra CTI con la stessa icona con uno
stile relativo alle dimensioni proporzionale al valore del volume/guadagno
lo spazio riservato alla icona dovrebbe essere quello della sua dimensione
maggiore in modo che i pulsanti non si allontanino o sia avvicinino durante la
selezione
--------------------------------------------------------------------------------
[OK] Rinominare gli script di post importarione e pre rimozione di un modulo
mediante cmd in: install.sh e uninstall.sh. aggiornare tutti i riferimenti ad
essi e propagare le modifiche in jms se necessario
--------------------------------------------------------------------------------
[OK] Rinominare i comandi di import e remove dei moduli di cmd in install e
uninstall. es. cmd module install
--------------------------------------------------------------------------------
Sezione "Chiamate" nella dashboard admin del modulo CTI.
Nuovo file js Admin (es. Calls.js) che visualizza lo storico delle chiamate
con paginazione e filtro per operatore. Per ogni chiamata è possibile
visualizzare i dettagli della chiamata in un pannello o modal dedicato.
--------------------------------------------------------------------------------
Verificare se è necessario aggiungere storage al gitignore della installazione
--------------------------------------------------------------------------------
Stabilire una procedura di gestione del repo del progetto corrente
--------------------------------------------------------------------------------
Verificare che l'installazione sia possibile anche se il repo di jms è privato
--------------------------------------------------------------------------------
Aggiungere alla dashboard root/admin al vista dei logs
--------------------------------------------------------------------------------
Aggiungere a dashboard admin gestione storage
--------------------------------------------------------------------------------
Implementare caricamenteo liste/contatti in CRM
--------------------------------------------------------------------------------
Il modulo CRM sostituise home
--------------------------------------------------------------------------------
[OK] Il click sul titolo non deve avere l'effetto di ricaricare la pagina come
gli altri link della applicazione in modo da non resettare la sessione nella
barra CTI.
--------------------------------------------------------------------------------
Implementare una gestione più robusta della sessione nella barra CTI per cercare
di evitare la caduta accidentale della connessione
--------------------------------------------------------------------------------
Testare su chrome l'installazione
--------------------------------------------------------------------------------
Aggiungere creazione/gestione campagne a CRM
--------------------------------------------------------------------------------
[OK] Verificare la possibilità di suddividere un modulo in componenti autonomi
aggiungiamo un subcommand cmd git init che nel progetto corrente esegue:
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/riccardovacirca/<project_name>.git
git push -u origin main
--------------------------------------------------------------------------------
Caricamneto liste contatti:
- L'associazione dei contatti ad una lista non è facoltativo
- deve esistere una lista di default cui vengono automaticamente associati i
  contatti non esplicitamente associati a una lista. questa lista di default può
  essere concettualmente una lista operatore e raccoglie tutti i contatti
  aggiunti al di fuori delle liste ufficiali.
- le liste sono caricate da utenti amministratori o root
- il wizard di importazione è presente insieme al crud di gestione liste nella
  dashboard dell'amministratore
