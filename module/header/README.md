# header

Barra di navigazione persistente. Si monta nel container `#header` all'avvio e rimane visibile in tutte le pagine. Mostra username e pulsante logout se autenticato, pulsante login altrimenti. Non aggiunge endpoint API.

## Endpoint

Nessuno.

## Import

```bash
cmd module import header
```
